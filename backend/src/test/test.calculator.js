/*
const total = 100
let eth = 63333
let inr=83.22


const coinToCurrency = (coin,currency) => {
    // results in 0.00 values 
    // value of coin in currency
    let total =  currency / coin
     //return Math.floor(total)
     return total
}
const CurrencyToCoin = (coin,currency) => {
    // results as how much value of coin in currency
    let total =  currency * coin
     //return Math.floor(total)
     return total
}

let coinToCurrencyresult = coinToCurrency(eth,inr)
let currencyToCoinresult = CurrencyToCoin(eth,inr)

console.log("coin: btc >>",eth);
console.log("currency:inr>> ",inr);
console.log("Value of coin in currency :",coinToCurrencyresult)
console.log("");
console.log("Value of currency in coin :",currencyToCoinresult)

*/


      